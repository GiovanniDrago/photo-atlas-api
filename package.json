{
  "name": "photo-atlas-api",
  "version": "0.1.0",
  "description": "Photo Atlas API: index and organize images and videos by time and space (Postgres/PostGIS, kDrive integration)",
  "type": "module",
  "private": true,
  "license": "GPL-3.0-only",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "node --watch src/server.js",
    "start": "node src/server.js",
    "migrate": "node scripts/migrate.js",
    "seed": "node scripts/seed.js",
    "reset-password": "node scripts/reset-password.js",
    "db:local": "bash scripts/db-local.sh",
    "test": "node --test test/"
  },
  "dependencies": {
    "@fastify/cors": "^11.3.0",
    "@fastify/rate-limit": "^11.2.0",
    "dotenv": "^17.4.2",
    "exifr": "^7.1.3",
    "fastify": "^5.12.4",
    "jose": "^6.2.12",
    "pg": "^8.23.0"
  },
  "devDependencies": {
    "supabase": "^2.16.1"
  }
}
